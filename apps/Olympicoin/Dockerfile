# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build Olympicoin`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t Olympicoin`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system Olympicoin && \
          adduser --system -G Olympicoin Olympicoin

COPY dist/apps/Olympicoin Olympicoin/
RUN chown -R Olympicoin:Olympicoin .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix Olympicoin --omit=dev -f install

CMD [ "node", "Olympicoin" ]
